<h2>Firewall Rules (iptables)</h2>
<pre><%= @iptables_output %></pre>

<p style="color: green"><%= notice %></p>

<h4>VPN Devices</h4>
<div id="vpn_devices">

  <table class="table table-striped table-hover">
    <thead class="table-light">
    <tr>
      <th>Row</th>
      <th>User</th>
      <th>Device description</th>
      <th colspan="2"></th>
    </tr>
    </thead>

    <tbody>
    <% @vpn_devices.each_with_index do |vpn_device,index| %>
      <tr>
        <td><%= index+1 %></td>
        <td><%= vpn_device.user.name %></td>
        <td><%= vpn_device.description %></td>

        <td><%= button_to 'Remove Device', vpn_device, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %></td>
        <td><%= button_to 'Show Device', vpn_device, data: { turbo_method: :get }, class: 'btn btn-primary btn-sm' %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
  <br>
</div>